var createSomething = (first:string, sec:number) =>{
    return `${sec.toString()} is the number of ${first}`
}

const some = createSomething('hello', 12)

console.log(some)

let obj:{
    name : string,
    age : number,
    isActive : boolean
}

obj = {
    name : 'Sarah',
    age : 21,
    isActive : false
}

// class objClass{f
//     constructor(
        
//     )
// }